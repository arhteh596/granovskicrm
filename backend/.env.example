# Backend environment example

# Use a full Postgres connection string from Neon/Supabase/Railway/Render
# Example (Neon):
# DATABASE_URL=postgres://user:password@host:5432/dbname
DATABASE_URL=

# Enable SSL for hobby cloud Postgres
DB_SSL=true
# Set to true only if your provider offers valid certificates and you want strict verification
DB_SSL_STRICT=false

# JWT secret for signing auth tokens
JWT_SECRET=change-me-please

# Optional: allowed frontend origins (comma-separated) if you deploy via serverless api/index.ts
# FRONTEND_ORIGINS=https://your-frontend.example.com
# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crm_calls_db
DB_USER=postgres
DB_PASSWORD=your_password

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Default Admin Credentials (hardcoded in code)
# admin / admin123

# File Upload
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# Push notifications VAPID keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT_EMAIL=mailto:admin@example.com
