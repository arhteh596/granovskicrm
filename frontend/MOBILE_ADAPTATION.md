# Мобильная Адаптация CRM - Документация

## Обзор

Данная CRM система была полностью адаптирована под мобильные устройства с использованием mobile-first подхода. Система корректно работает на экранах от 320px до больших desktop мониторов.

## Ключевые особенности

### 🎯 Mobile-First Подход
- Базовые стили написаны для мобильных устройств
- Прогрессивное улучшение для больших экранов
- Оптимизация для экранов 30-40% ширины браузера

### 📱 Адаптивные Breakpoints
- **320px-479px**: Очень маленькие экраны (старые телефоны)
- **480px-767px**: Современные телефоны
- **768px-1023px**: Планшеты
- **1024px-1199px**: Компактные desktop (30-40% ширины)
- **1200px+**: Полноразмерные desktop экраны

### ✋ Touch-Friendly Interface
- Минимальный размер кнопок: 44px × 44px
- Оптимизированные touch targets
- Предотвращение случайных нажатий
- Поддержка жестов

## Компоненты

### 1. MobileModal
Универсальный модальный компонент с адаптивным поведением.

```tsx
import { MobileModal } from '../components/ui';

<MobileModal
    isOpen={isOpen}
    onClose={() => setIsOpen(false)}
    title="Заголовок модалки"
    fullScreen={false} // Полный экран на мобильных
>
    <p>Содержимое модалки</p>
</MobileModal>
```

**Особенности:**
- На desktop: центрированная модалка
- На мобильных: slide-up снизу экрана
- Поддержка Safe Area для iPhone
- Автоблокировка скролла body

### 2. PhoneButtonsGrid
Адаптивная сетка телефонных кнопок.

```tsx
import { PhoneButtonsGrid } from '../components/ui';

<PhoneButtonsGrid
    phones={['+7 123 456-78-90', '+7 987 654-32-10']}
    onPhoneClick={(phone) => console.log('Calling:', phone)}
    variant="accent" // primary | secondary | accent
    size="medium" // small | medium | large
    maxColumns={4}
/>
```

**Адаптация:**
- 320px-479px: 1-2 колонки
- 480px-767px: 2-3 колонки
- 768px+: до maxColumns

### 3. StatusButtons
Адаптивные кнопки статусов.

```tsx
import { StatusButtons, getDefaultStatusButtons } from '../components/ui';

<StatusButtons
    buttons={getDefaultStatusButtons()}
    onStatusClick={(status, action) => {
        if (action === 'callback') {
            // Открыть модалку перезвона
        }
    }}
    maxColumns={4}
    size="medium"
    showIcons={true}
/>
```

**Адаптация:**
- Очень маленькие экраны: 1 колонка (полная ширина)
- Мобильные: 2 колонки
- Планшеты: 3 колонки
- Desktop: до maxColumns

## CSS Классы

### Utility Classes
```css
/* Отображение по устройствам */
.mobile-only         /* Только на мобильных */
.tablet-up           /* Планшеты и больше */
.desktop-only        /* Только desktop */
.hide-mobile         /* Скрыть на мобильных */

/* Мобильные утилиты */
.mobile-scroll       /* Touch-optimized скролл */
.mobile-no-select    /* Отключить выделение */
.mobile-tap-highlight/* Кастомный tap highlight */
.mobile-touch-action /* Touch action optimization */
```

### Responsive Grid
```css
.mobile-grid {
    display: grid;
    grid-template-columns: 1fr; /* Mobile по умолчанию */
    gap: 12px;
}

@media (min-width: 480px) {
    .mobile-grid {
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    }
}
```

## Примеры Адаптации

### Layout Component
Header автоматически адаптируется:
- Мобильные: бургер-меню + логотип + действия
- Desktop: полная навигация
- Компактный режим: иконки без текста

### Таблицы
```css
@media (max-width: 767px) {
    .table-responsive {
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
    }
    
    .hide-mobile-col {
        display: none; /* Скрыть неважные колонки */
    }
}
```

### Формы
```css
@media (max-width: 479px) {
    .form-input-mobile {
        font-size: 16px !important; /* Предотвращает zoom на iOS */
        min-height: 44px;
        padding: 12px 16px;
    }
}
```

## Safe Area Support

Поддержка выреза и скругленных углов современных телефонов:

```css
/* В CSS переменных */
--mobile-safe-area-top: env(safe-area-inset-top);
--mobile-safe-area-bottom: env(safe-area-inset-bottom);

/* Использование */
.mobile-safe-top {
    padding-top: max(var(--mobile-safe-area-top), 16px);
}
```

## Performance Оптимизации

### CSS
- Используется `transform` вместо изменения `left/top`
- `touch-action: manipulation` для устранения 300ms задержки
- `-webkit-overflow-scrolling: touch` для плавного скролла

### JavaScript
- Хуки `useResponsive*` для реактивности без лишних ререндеров
- Lazy loading компонентов
- Дебаунс для resize events

## Accessibility

### Touch Targets
- Минимум 44px × 44px для всех интерактивных элементов
- Достаточные отступы между элементами
- Фокус индикаторы для клавиатурной навигации

### Цвета и Контрастность
- Поддержка `prefers-color-scheme`
- Высококонтрастный режим `prefers-contrast: high`
- WCAG 2.1 AA compliant цвета

### Моторная Доступность
- `prefers-reduced-motion` support
- Отключение анимаций для пользователей с вестибулярными расстройствами

## Тестирование

### Browser DevTools
1. Открыть Developer Tools
2. Toggle Device Toolbar (Ctrl+Shift+M)
3. Тестировать различные размеры экранов
4. Проверить touch events

### Реальные Устройства
- iPhone SE (375×667) - минимальный современный размер
- iPhone 12/13 (390×844) - стандартный размер
- iPad (768×1024) - планшеты
- Компактный браузер (30-40% ширины на desktop)

### Автоматическое Тестирование
```bash
# Cypress тесты для разных viewport'ов
npx cypress run --config viewportWidth=375,viewportHeight=667
npx cypress run --config viewportWidth=768,viewportHeight=1024
```

## FAQ

### Q: Почему кнопки такие большие на мобильных?
A: Согласно Apple Human Interface Guidelines и Material Design, минимальный размер touch target должен быть 44px для удобного использования пальцами.

### Q: Можно ли отключить мобильную адаптацию?
A: Технически можно, но не рекомендуется. Можно настроить breakpoints в CSS переменных.

### Q: Как работает burger menu?
A: На экранах < 768px автоматически показывается кнопка меню. Навигация перемещается в выдвижную панель.

### Q: Поддерживаются ли жесты?
A: Базовые touch events поддерживаются. Для сложных жестов можно добавить библиотеку типа Hammer.js.

## Будущие Улучшения

- [ ] PWA поддержка с offline режимом
- [ ] Push уведомления для мобильных
- [ ] Drag & Drop для сортировки на touch устройствах  
- [ ] Virtual scrolling для больших списков
- [ ] Gesture navigation
- [ ] Haptic feedback для поддерживаемых устройств

---

**Автор:** GitHub Copilot с Claude Sonnet 4  
**Версия:** 1.0  
**Дата:** 17 декабря 2025