# Frontend environment example

# Base URL of your deployed backend API (must include /api)
# Example: https://crm-backend.onrender.com/api
VITE_API_URL=
VITE_API_URL=http://localhost:5000/api

# Public VAPID key for Push API (must match backend VAPID_PUBLIC_KEY)
VITE_VAPID_PUBLIC_KEY=
